---
"@type-atlas/mcp": patch
---

`occurrences` scans and reports files in lexicographic order instead of filesystem crawl order, so identical runs produce identical answers and results can be compared across changes.
