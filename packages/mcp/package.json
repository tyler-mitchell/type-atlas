{
  "name": "@featuretype/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "featuretype-mcp": "./bin/featuretype-mcp.cjs"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --outDir dist",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx ./src/dev.ts",
    "pretest": "pnpm --filter @featuretype/language-server build",
    "test": "vitest run --passWithNoTests",
    "pretest:integration": "pnpm --filter @featuretype/language-server build",
    "test:integration": "vitest run src/integration.test.ts",
    "smoke": "node --import tsx ./src/smoke.ts",
    "smoke:in-memory": "node --import tsx ./src/smoke.ts --transport=in-memory"
  },
  "dependencies": {
    "@featuretype/core": "workspace:*",
    "@featuretype/language-server": "workspace:*",
    "@featuretype/service": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@volar/language-core": "2.4.28",
    "@volar/language-service": "2.4.28",
    "@volar/typescript": "2.4.28",
    "citty": "^0.2.2",
    "volar-service-typescript": "0.0.65",
    "vscode-languageserver-protocol": "^3.17.5",
    "vscode-uri": "^3.0.8",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
