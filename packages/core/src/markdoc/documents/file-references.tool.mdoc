---
{ "tool": "file_references" }
---

{% if equals($total, 0) %}
Nothing references {% $file %}. This answer is assembled from references to what the module declares, so a module that declares nothing, or one imported only for its side effects, reports none even when other files import it.
{% /if %}
{% if not(equals($total, 0)) %}
{% $file %} · referenced from {% $total %} {% plural count=$total forms={"one": "place", "other": "places"} /%} · {% $projects %} {% plural count=$projects forms={"one": "project", "other": "projects"} /%} loaded{% if any($anchor) %} · {% $anchor %}{% /if %}
{% partial file="page.mdoc" /%}

{% tree entries=$groups partial="reference-node.mdoc" /%}
{% /if %}
