---
tool: completions
---

{% if equals($total, 0) %}
Nothing completes at {% $at %} in {% $file %}. Completions answer where an identifier can be written, so a position inside a string, a comment, or whitespace reports none.
{% /if %}
{% if not(equals($total, 0)) %}
Completions at {% $at %} in {% $file %}{% if $incomplete %} · the list is incomplete, so typing more narrows it{% /if %}.
{% partial file="page.mdoc" /%}

{% tree entries=$items /%}
{% /if %}
