---
tool: inlay_hints
---

{% if equals($total, 0) %}
Nothing in {% $at %} of {% $file %} has an inferred detail to show. Inlay hints name what the source leaves implicit — a parameter's name at a call, an inferred type — so a range with neither reports none.
{% /if %}
{% if not(equals($total, 0)) %}
{% $file %}:{% $at %} · {% $total %} {% plural count=$total forms={"one": "hint", "other": "hints"} /%}

{% tree entries=$hints /%}
{% /if %}
