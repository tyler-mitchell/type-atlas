---
tool: document_highlights
---

{% if equals($total, 0) %}
Nothing at this position is highlighted in {% $file %}. Highlights are same-document usages, so a position that names no symbol — whitespace, punctuation, a comment — reports none.
{% /if %}
{% if not(equals($total, 0)) %}
{% if any($subject) %}{% $subject %}{% if $kind %} [{% $kind %}]{% /if %} · {% /if %}{% $total %} {% plural count=$total forms={"one": "use", "other": "uses"} /%} in this file

{% tree entries=$groups partial="reference-node.mdoc" /%}
{% /if %}
