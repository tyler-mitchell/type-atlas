---
tool: references
---

{% if equals($total, 0) %}
Nothing was found at this position, so there is no symbol to report references for. Check the line and character — both are one-based, and they must land on the symbol's own name.
{% /if %}
{% if not(equals($total, 0)) %}
{% $subject %} is {% if $kind %}{% article($kind) %}{% /if %}{% if not($kind) %}a symbol{% /if %}{% if $container %} declared inside {% $container %}{% /if %}{% if $declaredAt %}, at {% $declaredAt.file %}:{% position($declaredAt.at) %}{% /if %}.
Searched {% if $everyProject %}every project loaded this session{% if any($anchor) %}, anchored at {% $anchor %}{% /if %}{% /if %}{% if not($everyProject) %}{% if any($anchor) %}the project {% $anchor %}{% /if %}{% if not(any($anchor)) %}the project inferred for this file{% /if %}{% /if %} and found {% $total %} {% plural count=$total forms={"one": "reference", "other": "references"} /%}.
{% partial file="page.mdoc" /%}

{% if $noUses %}
Nothing references it. If you expected otherwise, the projects holding those uses may not be loaded yet — open a file in one and ask again.
{% /if %}
{% if not($noUses) %}
{% tree entries=$groups partial="reference-node.mdoc" /%}
{% /if %}
{% /if %}
