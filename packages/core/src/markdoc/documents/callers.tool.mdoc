---
{ "tool": "callers" }
---

{% if not($name) %}
Nothing at this position is callable, so nothing can call it. Callers reports what invokes a function or method, and the call hierarchy prepared none here.
{% /if %}
{% if $name %}
{% if equals($total, 0) %}
{% $name %} is called from nowhere in the {% $projects %} relevant {% plural count=$projects forms={"one": "project", "other": "projects"} /%} searched. A caller is a call from inside a declaration, so a re-export or a bare mention is not one — use references for every mention, including those.
{% /if %}
{% if not(equals($total, 0)) %}
{% $name %} · called from {% $total %} {% plural count=$total forms={"one": "place", "other": "places"} /%} · {% $projects %} relevant {% plural count=$projects forms={"one": "project", "other": "projects"} /%} searched

{% tree entries=$groups partial="call-node.mdoc" /%}
{% /if %}
{% /if %}
