---
{ "tool": "verify_edit" }
---

{% if equals($introducedCount, 0) %}
The proposed {% plural count=$fileCount forms={"one": "edit introduces", "other": "edits introduce"} /%} no problem the {% $fileCount %} {% plural count=$fileCount forms={"one": "file does", "other": "files do"} /%} not already have{% if any($resolvedCount) %}, and {% plural count=$resolvedCount forms={"one": "resolves one standing problem", "other": "resolve standing problems"} /%}{% /if %}. Only these files were checked — a change can also break importers, and diagnostics after applying reports those.
{% /if %}
{% if not(equals($introducedCount, 0)) %}
The proposed {% plural count=$fileCount forms={"one": "edit introduces", "other": "edits introduce"} /%} {% $introducedCount %} {% plural count=$introducedCount forms={"one": "problem", "other": "problems"} /%}{% if any($resolvedCount) %} and {% plural count=$resolvedCount forms={"one": "resolves one", "other": "resolve some"} /%}{% /if %}, before anything is written.

{% each items=$groups as="group" partial="diagnostic-group.mdoc" /%}
{% /if %}
