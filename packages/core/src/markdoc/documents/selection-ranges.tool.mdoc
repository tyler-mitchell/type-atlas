---
{ "tool": "selection_ranges" }
---

{% if equals($total, 0) %}
Nothing at these positions expands to a range in {% $file %}. A selection expands outward from a symbol, so a position in whitespace or past the end of a line reports none.
{% /if %}
{% if not(equals($total, 0)) %}
{% $file %} · {% $fromCount %} {% plural count=$fromCount forms={"one": "position expands", "other": "positions expand"} /%} through {% $total %} {% plural count=$total forms={"one": "range", "other": "ranges"} /%} · each contains the one above it

{% tree entries=$chains /%}
{% /if %}
