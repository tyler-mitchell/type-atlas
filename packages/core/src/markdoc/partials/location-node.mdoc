{% if not($node.selection) %}{% $node.file %}{% /if %}{% if $node.selection %}{% if $node.name %}{% $node.name %}{% if $node.kind %} [{% $node.kind %}]{% /if %} · {% /if %}{% if $node.file %}{% $node.file %}:{% /if %}{% range($node.selection) %}{% if $node.extent %} · range {% range($node.extent) %}{% /if %}{% if $node.within %} · inside {% $node.within %}{% /if %}{% if $node.detail %} — {% $node.detail %}{% /if %}{% if $node.text %}:  {% $node.text %}{% /if %}{% /if %}
