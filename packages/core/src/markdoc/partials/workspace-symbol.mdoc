{% truncate value=$item.name /%} [{% symbolKind($item.kind) %}]{% if $item.deprecated %} [deprecated]{% /if %} · {% $item.file %}{% if $item.range %}:{% range($item.range) %}{% /if %}{% if $item.container %} — {% truncate value=$item.container /%}{% /if %}
