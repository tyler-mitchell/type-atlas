{% truncate value=$item.name /%} [{% if $item.word %}{% $item.word %}{% /if %}{% if not($item.word) %}{% symbolKind($item.kind) %}{% /if %}]{% if $item.deprecated %} [deprecated]{% /if %} · {% $item.file %}{% if $item.range %}:{% range($item.range) %}{% /if %}{% if $item.container %} — {% truncate value=$item.container /%}{% /if %}
