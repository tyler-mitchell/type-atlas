{% $node.name %} [{% symbolKind($node.kind) %}] {% range($node.selection) %}{% if $node.extent %} · range {% range($node.extent) %}{% /if %}{% if $node.folded %} · {% $node.folded %} entries{% /if %}{% if $node.detail %} — {% $node.detail %}{% /if %}
