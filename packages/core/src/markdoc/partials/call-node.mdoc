{% if not($node.name) %}{% $node.file %}{% /if %}{% if $node.name %}{% $node.name %} [{% symbolKind($node.kind) %}] {% if $node.file %}{% $node.file %}:{% /if %}{% range($node.selection) %}{% if $node.extent %} · range {% range($node.extent) %}{% /if %} · calls {% if $node.siteFile %}{% $node.siteFile %}:{% /if %}{% list($node.sites) %}{% if $node.detail %} — {% $node.detail %}{% /if %}{% /if %}
