# Single Select Combobox

Single-select combobox for toolbar filter controls.

```tsx
// ./single-select-combobox.examples.tsx
import { useState } from "react"
import { SingleSelectCombobox } from "./components"

export function MinimalControlledCombobox() {
  const [value, setValue] = useState("updated-desc")
  return (
    <SingleSelectCombobox
      ariaLabel="Sort repos"
      placeholder="Sort"
      value={value}
      onValueChange={setValue}
      options={[
        { label: "Recently updated", value: "updated-desc" },
        { label: "Name A-Z", value: "name-asc" },
      ]}
    />
  )
}
```

```tsx
// ./single-select-combobox.showcase.tsx
import { useState } from "react"
import { SingleSelectCombobox } from "./components"

export function ToolbarShowcase({
  size,
  variant,
}: {
  size: "sm" | "md"
  variant: "default" | "muted"
}) {
  const [value, setValue] = useState("updated-desc")
  return (
    <div data-size={size} data-variant={variant}>
      <SingleSelectCombobox
        ariaLabel="Repo filters"
        placeholder="Filter"
        value={value}
        onValueChange={setValue}
        options={[
          { label: "Recently updated", value: "updated-desc" },
          { label: "Name A-Z", value: "name-asc" },
        ]}
      />
    </div>
  )
}
```
